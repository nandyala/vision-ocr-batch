<?xml version="1.0" encoding="UTF-8"?>
<!--
  Vision OCR - parent build.
    ocr-batch : the Spring Batch job (pure XML configuration) that calls Azure Document Intelligence
    ocr-ui    : Spring Boot demo web app (upload, review/corrections, data explorer, configuration).
                It runs the same batch job in-process, so an upload is processed immediately.

  Build everything:  mvn -DskipTests package
    ocr-batch/target/vision-ocr-batch.jar   (runnable batch job)
    ocr-ui/target/vision-ocr-ui.jar         (runnable demo web app)
-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.visionocr</groupId>
    <artifactId>vision-ocr-parent</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <packaging>pom</packaging>
    <name>Vision OCR</name>

    <modules>
        <module>ocr-batch</module>
        <module>ocr-ui</module>
    </modules>

    <properties>
        <java.version>17</java.version>
        <maven.compiler.release>17</maven.compiler.release>
        <!-- keep method parameter names in the class files (Spring MVC / Boot rely on them) -->
        <maven.compiler.parameters>true</maven.compiler.parameters>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    </properties>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>3.13.0</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>3.5.2</version>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>
</project>
