<?xml version="1.0" encoding="UTF-8"?>
<!-- DataSource, transactions, Spring Batch job repository/launcher, schema init. -->
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:jdbc="http://www.springframework.org/schema/jdbc"
       xsi:schemaLocation="
         http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd
         http://www.springframework.org/schema/jdbc  https://www.springframework.org/schema/jdbc/spring-jdbc.xsd">

    <bean id="dataSource" class="com.zaxxer.hikari.HikariDataSource" destroy-method="close">
        <property name="jdbcUrl" value="${db.url}"/>
        <property name="driverClassName" value="${db.driver}"/>
        <property name="username" value="${db.user}"/>
        <property name="password" value="${db.password}"/>
        <property name="maximumPoolSize" value="${db.pool-size}"/>
    </bean>

    <bean id="transactionManager" class="org.springframework.jdbc.support.JdbcTransactionManager">
        <property name="dataSource" ref="dataSource"/>
    </bean>

    <bean id="jdbcTemplate" class="org.springframework.jdbc.core.JdbcTemplate">
        <property name="dataSource" ref="dataSource"/>
    </bean>

    <!-- Creates the job's objects in the dedicated schema "ocr" if they are missing (db.init=true), then
         logs database/login and verifies the tables. Create-only scripts: nothing is dropped or altered,
         other schemas are never touched. -->
    <bean id="schemaInitializer" class="com.visionocr.config.SchemaInitializer" init-method="initialize">
        <constructor-arg index="0" ref="dataSource"/>
        <constructor-arg index="1" value="${db.init}"/>
        <constructor-arg index="2">
            <list>
                <value>classpath:schema-batch-sqlserver.sql</value>
                <value>classpath:schema-app-sqlserver.sql</value>
            </list>
        </constructor-arg>
    </bean>

    <!-- Spring Batch metadata lives in ocr.BATCH_* (not dbo), so it cannot clash with other applications -->
    <bean id="jobRepository" class="org.springframework.batch.core.repository.support.JobRepositoryFactoryBean"
          depends-on="schemaInitializer">
        <property name="dataSource" ref="dataSource"/>
        <property name="transactionManager" ref="transactionManager"/>
        <property name="databaseType" value="SQLSERVER"/>
        <property name="tablePrefix" value="ocr.BATCH_"/>
    </bean>

    <bean id="jobExplorer" class="org.springframework.batch.core.explore.support.JobExplorerFactoryBean">
        <property name="dataSource" ref="dataSource"/>
        <property name="transactionManager" ref="transactionManager"/>
        <property name="tablePrefix" value="ocr.BATCH_"/>
    </bean>

    <!-- Threads for the classify/extract partitions -->
    <bean id="batchTaskExecutor" class="org.springframework.scheduling.concurrent.ThreadPoolTaskExecutor">
        <property name="corePoolSize" value="${batch.threads}"/>
        <property name="maxPoolSize" value="${batch.threads}"/>
        <property name="threadNamePrefix" value="ocr-"/>
    </bean>

    <bean id="jobLauncher" class="org.springframework.batch.core.launch.support.TaskExecutorJobLauncher">
        <property name="jobRepository" ref="jobRepository"/>
    </bean>

</beans>
