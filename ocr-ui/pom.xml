<?xml version="1.0" encoding="UTF-8"?>
<!--
  Demo web app (Spring Boot). Runs the batch job from ocr-batch in-process:
  upload -> the job runs -> results, review and corrections in the browser.
  The batch module itself stays pure XML; this module uses normal Spring Boot conventions.
-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.visionocr</groupId>
        <artifactId>vision-ocr-parent</artifactId>
        <version>0.1.0-SNAPSHOT</version>
    </parent>

    <artifactId>vision-ocr-ui</artifactId>
    <packaging>jar</packaging>
    <name>Vision OCR - Demo UI (Spring Boot)</name>

    <properties>
        <!-- Spring Boot 3.5 = Spring Framework 6.2 + Spring Batch 5.2, the same generation as ocr-batch -->
        <spring-boot.version>3.5.16</spring-boot.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring-boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <!-- The batch job, its XML configuration (DataSource, JdbcTemplate, transactions, job), SQL scripts
             and doc types -->
        <dependency>
            <groupId>com.visionocr</groupId>
            <artifactId>vision-ocr-batch</artifactId>
            <version>${project.version}</version>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <finalName>vision-ocr-ui</finalName>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <version>${spring-boot.version}</version>
                <configuration>
                    <mainClass>com.visionocr.ui.OcrUiApplication</mainClass>
                </configuration>
                <executions>
                    <execution>
                        <goals><goal>repackage</goal></goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
